<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.titaniel.zerobasedbudgetingapp">

    <application
        android:name=".api.ZeroBasedBudgetingApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZeroBasedBudgetingApp">
        <activity
            android:exported="false"
            android:name=".compose.HostActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AddEditTransactionActivity" />
        <activity android:name=".activities.ManageCategoriesActivity" />
        <activity android:name="._testing.HiltTestActivity" />
    </application>

</manifest>